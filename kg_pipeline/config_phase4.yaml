# Example config for Phase 4 expansion (90 days, 50–100 papers).
# Copy relevant keys into config.yaml or override with this file when running.
# Usage: RUN_ID=phase4 python run_pipeline.py  (and point config loader to this file if desired)
fetch:
  days_back: 90
  max_results: 50
  topic_keywords: []
  aging_keywords: ["aging", "sarcopenia", "bone health", "life course"]
